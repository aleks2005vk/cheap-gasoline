# 🎉 Cheap Gasoline Backend - ГОТОВО!

## ✅ ЧТО РАБОТАЕТ

### Сервер (FastAPI)
- **Порт:** 8001 (http://127.0.0.1:8001)
- **Статус:** Запущен ✅
- **БД:** SQLite (backend/cheap_gasoline.db) с 562 станциями

### API Endpoints
- ✅ **GET /api/stations** - Список всех 562 станций
- ✅ **POST /api/auth/register** - Регистрация новых пользователей (возвращает JWT токен)
- ✅ **POST /api/auth/login** - Логин пользователей (возвращает JWT токен)
- ✅ **POST /api/stations/{id}/prices** - Добавление цен (требует JWT токен)
- ✅ **POST /api/upload-photo** - Загрузка фотографий с OCR

### Аутентификация
- ✅ JWT токены (7 дней)
- ✅ Хеширование паролей (bcrypt)
- ✅ Валидация пароля (макс 72 байта)

### Данные
- ✅ 562 станции заправок в БД (Грузия, Тбилиси)
- ✅ Структура: ID, название, бренд, координаты (lat/lng), адрес

---

## 🚀 КАК ЗАПУСТИТЬ

### 1️⃣ Запустить фронтенд
```bash
npm run dev
```
Откроется на http://localhost:5176

### 2️⃣ Запустить сервер (в отдельном терминале)
```bash
npm run backend:start
```
или с горячей перезагрузкой:
```bash
npm run backend:dev
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Регистрация
```bash
curl -X POST http://127.0.0.1:8001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user@test.com","password":"pass123","name":"User"}'
```

### Логин
```bash
curl -X POST http://127.0.0.1:8001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user@test.com","password":"pass123"}'
```

### Получить станции
```bash
curl http://127.0.0.1:8001/api/stations
```

---

## 📝 ПРИМЕРЫ ОТВЕТОВ

### Регистрация ✅
```json
{
  "user": {
    "id": 1,
    "email": "test@example.com",
    "name": "Test User",
    "avatar": null
  },
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### Список станций ✅
```json
[
  {
    "id": 1,
    "name": "Wissol Premium Station 1",
    "brand": "Wissol Premium Station 1",
    "lat": 41.793563,
    "lng": 44.770812,
    "address": "премиум-станция Wissol",
    "osm_id": null
  },
  ...562 stations total
]
```

---

## 🔧 КОНФИГУРАЦИЯ

### Фронтенд (vite.config.js)
- Proxy: `/api` → `http://127.0.0.1:8001`

### БД (SQLite)
- Файл: `backend/cheap_gasoline.db`
- Переключение на PostgreSQL: установить `BACKEND_DATABASE_URL` env var

### Tesseract OCR
- Auto-detected: `C:\Users\User\AppData\Local\Programs\Tesseract-OCR\tesseract.exe`

---

## 📦 ТРЕБОВАНИЯ

✅ Python 3.12 (venv создан)
✅ Node.js + npm
✅ Tesseract-OCR (установлен локально)

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ (опционально)

1. Переключить на PostgreSQL (указать `BACKEND_DATABASE_URL`)
2. Создать demo-пользователя при старте
3. Обновить MapComponent чтобы загружал станции из API
4. Добавить UI для загрузки фото и добавления цен

---

**СТАТУС: ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ!**
